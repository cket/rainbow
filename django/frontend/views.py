from django.shortcuts import render
from django.http import HttpResponse


def index(request):

	#call script here
	#rewrite the response string to be yes/no
	#include some info about caller(location, time, weather)
    return HttpResponse("penis butt")
